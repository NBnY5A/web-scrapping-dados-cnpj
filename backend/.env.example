# Configuração do PostgreSQL
POSTGRES_DB=cnpj_database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123

# String de conexão (ajuste o usuário se necessário)
# Para instalação local via Homebrew ou Postgres.app, pode não precisar de senha
DATABASE_URL=postgresql+psycopg2://postgres@localhost:5432/cnpj_database
